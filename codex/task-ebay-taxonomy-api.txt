## タスク: Phase 1-A - eBay Taxonomy API実装（getItemAspectsForCategory）

### 概要
eBay Taxonomy APIの`getItemAspectsForCategory`を`apps/worker/src/lib/ebay-api.ts`に実装する。
カテゴリごとの必須Item Specificsを動的取得し、DBにキャッシュする。

### 実装1: `getItemAspectsForCategory()` メソッド追加

#### apps/worker/src/lib/ebay-api.ts の EbayApiClient クラスに追加:

```typescript
/**
 * カテゴリごとのItem Aspects（Item Specifics）を取得（Taxonomy API）
 * GET /commerce/taxonomy/v1/category_tree/{categoryTreeId}/get_item_aspects_for_category?category_id={categoryId}
 */
async getItemAspectsForCategory(
  categoryId: string,
  categoryTreeId: string = '0'
): Promise<EbayApiResponse<{
  aspects: Array<{
    localizedAspectName: string;
    aspectConstraint: {
      aspectRequired: boolean;
      aspectMode: string;  // 'FREE_TEXT' | 'SELECTION_ONLY'
    };
    aspectValues?: Array<{ localizedValue: string }>;
  }>;
}>>
```

実装パターンは既存の `searchCategories()` メソッド（830行目付近）と同じ:
- `this.accessToken` でトークン取得（ensureAccessTokenは呼ばなくてよい、既にクラスで管理）
  - ただし、accessTokenがnullの場合は `await this.ensureAccessToken()` を呼ぶ
- 直接fetchで `https://api.ebay.com/commerce/taxonomy/v1/category_tree/${categoryTreeId}/get_item_aspects_for_category?category_id=${categoryId}` にGET
- Authorization: `Bearer ${accessToken}`
- レスポンスの `aspects` 配列を返す
- エラー時は `{ success: false, error: { code, message } }` を返す

### 実装2: `getDefaultCategoryTreeId()` 静的メソッド追加

```typescript
/**
 * マーケットプレイスIDからデフォルトのカテゴリツリーIDを取得
 */
static getDefaultCategoryTreeId(marketplaceId: string = 'EBAY_US'): string {
  const treeIdMap: Record<string, string> = {
    'EBAY_US': '0',
    'EBAY_GB': '3',
    'EBAY_DE': '77',
    'EBAY_AU': '15',
    'EBAY_FR': '71',
    'EBAY_IT': '101',
    'EBAY_ES': '186',
    'EBAY_CA': '2',
  };
  return treeIdMap[marketplaceId] || '0';
}
```

### 実装3: `fetchAndCacheItemAspects()` メソッド追加

DBキャッシュ付きのItem Aspects取得メソッド:

```typescript
/**
 * Item Aspectsを取得してDBにキャッシュする
 * 1. DBのEbayCategoryMappingからキャッシュを確認（updatedAt + 24h以内）
 * 2. キャッシュがあればそれを返す
 * 3. なければTaxonomy APIから取得してDBに保存
 */
async fetchAndCacheItemAspects(
  categoryId: string,
  marketplaceId: string = 'EBAY_US'
): Promise<{
  aspects: Array<{
    localizedAspectName: string;
    required: boolean;
    mode: string;
    values: string[];
  }>;
}>
```

実装:
1. `prisma.ebayCategoryMapping.findFirst({ where: { ebayCategoryId: categoryId } })` でDBキャッシュ確認
2. キャッシュがあり、`updatedAt` が24時間以内なら `itemSpecifics` JSONから返す
   - itemSpecificsの形式: `{ aspects: [...], fetchedAt: ISO string }`
3. キャッシュなしまたは古い場合:
   - `getItemAspectsForCategory(categoryId, treeId)` を呼ぶ
   - 結果をパースして簡略化: `{ localizedAspectName, required: aspectConstraint.aspectRequired, mode: aspectConstraint.aspectMode, values: aspectValues?.map(v => v.localizedValue) || [] }`
   - `prisma.ebayCategoryMapping.upsert()` で保存
     - where: `{ ebayCategoryId: categoryId }`（ユニークでない場合は findFirst → update/create）
     - update: `{ itemSpecifics: { aspects: [...], fetchedAt: new Date().toISOString() } }`
     - create: `{ ebayCategoryId: categoryId, ebayCategoryName: 'Category ' + categoryId, sourceCategory: 'auto:' + categoryId, itemSpecifics: { aspects: [...], fetchedAt: new Date().toISOString() } }`

### 実装4: ebay-publish.ts の processCreateInventoryItem を修正

`apps/worker/src/processors/ebay-publish.ts` の `processCreateInventoryItem()` 関数（71行目付近）を修正。
現在のItem Specifics構築ロジック（107-133行目）の前に、Taxonomy APIからの必須項目取得を追加:

```typescript
// 修正箇所: 109行目の `let aspects: Record<string, string[]> = marketplaceData.itemSpecifics || {};` の前に追加

// Taxonomy APIから必須Item Specificsを取得
let requiredAspects: Array<{ localizedAspectName: string; required: boolean; mode: string; values: string[] }> = [];
const categoryId = marketplaceData.categoryId;
if (categoryId) {
  try {
    const cached = await ebayApi.fetchAndCacheItemAspects(categoryId);
    requiredAspects = cached.aspects.filter(a => a.required);
    log.info({ type: 'fetched_item_aspects', categoryId, totalAspects: cached.aspects.length, requiredCount: requiredAspects.length });
  } catch (error: any) {
    log.warn({ type: 'fetch_item_aspects_failed', categoryId, error: error.message });
  }
}
```

そして、既存のaspects構築ロジック（112-133行目）の後に、必須項目の充足チェックを追加:

```typescript
// 必須Item Specificsが未設定の場合、デフォルト値を補完
for (const reqAspect of requiredAspects) {
  if (!aspects[reqAspect.localizedAspectName]) {
    if (reqAspect.values.length > 0 && reqAspect.mode === 'SELECTION_ONLY') {
      // SELECTION_ONLYの場合、選択肢から最初の値は使わない（不正確なため）
      // "N/A" や "Does not apply" で埋める
      aspects[reqAspect.localizedAspectName] = ['N/A'];
    } else {
      aspects[reqAspect.localizedAspectName] = ['N/A'];
    }
    log.info({ type: 'filled_missing_aspect', aspect: reqAspect.localizedAspectName, value: 'N/A' });
  }
}
```

### 注意事項
- EbayCategoryMapping モデルのitemSpecificsフィールドは `Json` 型で既存。スキーマ変更不要。
- `searchCategories()` メソッド（830行目付近）のパターンをそのまま踏襲する
- `EBAY_API_BASE` ではなく直接 `https://api.ebay.com` を使っている（searchCategoriesと同様）
  - ただし本番/サンドボックス判定がある場合は、ファイル先頭のIS_PRODUCTION定数を確認して適切なURLを使う
- テストが壊れないようにする
- 修正後に `cd ~/Desktop/rakuda && npm test --workspace=apps/worker` で全テストが通ることを確認
- `EbayCategoryMapping` のユニーク制約は `sourceCategory` にある。`ebayCategoryId` はユニークではない可能性があるので、findFirst → update or create パターンを使う
