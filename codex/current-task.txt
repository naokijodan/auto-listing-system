Task: Phase 0-C-4 - Migrate joom-publish-service.ts from JoomListing to Listing

File: apps/worker/src/lib/joom-publish-service.ts

Replace all prisma.joomListing operations with prisma.listing operations using marketplace='JOOM'.

Key changes:
1. prisma.joomListing.upsert({where: {taskId}}) -> prisma.listing.upsert({where: {productId_marketplace: {productId, marketplace: 'JOOM'}}})
2. prisma.joomListing.findUnique({where: {id}}) -> prisma.listing.findUnique({where: {id}})
3. prisma.joomListing.update() -> prisma.listing.update()
4. prisma.joomListing.delete() -> prisma.listing.delete()

Data mapping:
- JoomListing.title/description/joomProductId/joomVariantId/joomSku/joomCategory/joomAttributes/joomImages/shippingPrice/shippingTime/dryRunResult/errorCount/lastError -> Listing.marketplaceData JSON
- JoomListing.price -> Listing.listingPrice
- JoomListing.status -> Listing.status (DRAFT->DRAFT, READY->PENDING_PUBLISH, PUBLISHING->PUBLISHING, ACTIVE->ACTIVE, PAUSED->PAUSED, SOLD->SOLD, ENDED->ENDED, ERROR->ERROR)
- JoomListing.publishedAt -> Listing.publishedAt
- JoomListing.lastSyncedAt -> Listing.lastSyncedAt
- JoomListing.currency -> Listing.currency

For marketplaceData updates, merge with existing:
```typescript
const existing = await prisma.listing.findUnique({where: {id}});
const currentData = (existing?.marketplaceData as any) || {};
await prisma.listing.update({
  where: {id},
  data: {marketplaceData: {...currentData, ...newData}}
});
```

For queries checking `joomListing: null` on enrichmentTask, change to:
```typescript
// Check if no Listing exists for this product with JOOM marketplace
const existingListing = await prisma.listing.findFirst({
  where: { productId: task.productId, marketplace: 'JOOM' }
});
if (existingListing) continue; // skip if already has a Joom listing
```

Also update the helper functions to extract data from marketplaceData:
- listing.joomProductId -> (listing.marketplaceData as any)?.joomProductId
- listing.title -> (listing.marketplaceData as any)?.title
- etc.

DO NOT modify schema.prisma. DO NOT run prisma commands. Only edit joom-publish-service.ts.
After editing, run: cd ~/Desktop/rakuda && npm test --workspace=apps/worker 2>&1 | tail -30
