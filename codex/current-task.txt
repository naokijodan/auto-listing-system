以下の仕様でTypeScriptファイルを4つ生成してください。

## 出力先
/Users/naokijodan/Desktop/rakuda/codex/output/

## 共通ルール
- Express Router形式（import { Router } from 'express'）
- 各API: 28エンドポイント
- 各UI: 6タブ、shadcn/ui使用、SWRでfetch
- UIファイル先頭に 'use client'; を必ず入れる

---

## Phase 313: Inventory Sync Manager（在庫同期マネージャー）

### API: ebay-inventory-sync.ts
- テーマカラー: emerald-600
- ダッシュボード (5): /dashboard, /dashboard/summary, /dashboard/sync-status, /dashboard/errors, /dashboard/activity
- 在庫同期 (6): /syncs (CRUD + start + stop)
- チャンネル (4): /channels, /channels/:id, /channels/connect, /channels/test
- マッピング (4): /mappings, /mappings/:id, /mappings/create, /mappings/auto-match
- 分析 (3): /analytics, /analytics/accuracy, /analytics/performance
- 設定 (2): /settings GET/PUT
- ユーティリティ (4): /health, /export, /import, /force-sync

### UI: inventory-sync-page.tsx
- h1: "在庫同期マネージャー" (text-emerald-600)
- タブ: ダッシュボード、同期、チャンネル、マッピング、分析、設定
- fetch先: /api/ebay-inventory-sync/...

---

## Phase 314: Price Alerts（価格アラート）

### API: ebay-price-alerts.ts
- テーマカラー: sky-600
- ダッシュボード (5): /dashboard, /dashboard/summary, /dashboard/active-alerts, /dashboard/triggered, /dashboard/trends
- アラート (6): /alerts (CRUD + enable + test)
- ルール (4): /rules, /rules/:id, /rules/create, /rules/priority
- 通知 (4): /notifications, /notifications/:id, /notifications/send, /notifications/channels
- 分析 (3): /analytics, /analytics/effectiveness, /analytics/products
- 設定 (2): /settings GET/PUT
- ユーティリティ (4): /health, /export, /import, /check-prices

### UI: price-alerts-page.tsx
- h1: "価格アラート" (text-sky-600)
- タブ: ダッシュボード、アラート、ルール、通知、分析、設定
- fetch先: /api/ebay-price-alerts/...

---

完了したらファイルパスのみ報告してください。
