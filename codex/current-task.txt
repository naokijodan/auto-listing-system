apps/api/src/index.ts（804行）をリファクタリングしてください。

## 目的
index.tsが肥大化しているため、ルーター登録を分割ファイルに切り出す。

## 出力先
/Users/naokijodan/Desktop/rakuda/codex/output/

## 生成するファイル

### 1. routes/ebay-routes.ts
eBay系ルーター（248件）のimportとapp.use()をまとめたファイル。

```typescript
import { Express } from 'express';

export function registerEbayRoutes(app: Express): void {
  // 各eBayルーターのimportとapp.use()をここにまとめる
  // 例:
  // const ebayAuthRouter = require('./ebay-auth').default;
  // app.use('/api/ebay', ebayAuthRouter);

  // 動的importではなく、既存のstatic importパターンを維持
  // ただしimport文はファイル先頭に、app.use()はregisterEbayRoutes関数内に配置
}
```

importは全てファイル先頭にstatic importで配置し、関数内でapp.use()を呼ぶ形式にしてください。

### 2. routes/core-routes.ts
コア系ルーター（health, products, listings, jobs, admin等のeBay以外）のimportとapp.use()をまとめたファイル。

```typescript
import { Express } from 'express';

export function registerCoreRoutes(app: Express): void {
  // コア系ルーターのapp.use()をここにまとめる
}
```

### 3. index.ts.new（リファクタリング後のindex.ts）
上記2ファイルを使って簡潔にしたindex.ts。以下の構造を維持:
- dotenv, express, cors, helmet等のインフラimport
- Redis, BullMQ, Bull Board セットアップ
- ミドルウェア設定
- Swagger設定
- registerCoreRoutes(app) 呼び出し
- registerEbayRoutes(app) 呼び出し
- Bull Board、エラーハンドラー
- サーバー起動、Graceful Shutdown

## 重要ルール
- 現在のindex.tsの全import文と全app.use()文を漏れなく移行すること
- 既存の動作を一切変更しないこと（純粋なリファクタリング）
- eBay系は「ebay」がパス名に含まれるものすべて
- コア系はそれ以外すべて

## 参照
現在のindex.tsは /Users/naokijodan/Desktop/rakuda/apps/api/src/index.ts にあります。

完了したらファイルパスのみ報告してください。
