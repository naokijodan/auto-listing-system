## タスク: テスト失敗3件の修正

### 失敗テスト

#### 1. listings.test.ts "should return 400 for invalid request body"
- ファイル: `apps/api/src/test/integration/listings.test.ts`
- 問題: POSTリクエストが400ではなく別のステータスを返す
- 修正: テストのmock設定またはリクエストボディを実装に合わせる
- ルート実装: `apps/api/src/routes/listings.ts` を確認

#### 2. listing-performance.test.ts "should accept request body without error"
- ファイル: `apps/api/src/test/unit/listing-performance.test.ts`
- 問題: 5003msタイムアウト
- 修正: テスト内のmock設定が不足していてレスポンスが返らない可能性。mockを追加するか、タイムアウトを延ばすか確認
- ルート実装: `apps/api/src/routes/listing-performance.ts` を確認

#### 3. settings.test.ts Unhandled Rejection
- ファイル: `apps/api/src/test/integration/settings.test.ts` line 250
- 問題: `mockRejectedValue`で投げたErrorがUnhandled Rejectionになる
- 原因: `mockPrisma.marketplaceSyncSetting.upsert.mockRejectedValue(new Error('DB Error'))` — テスト対象のルートがPromiseを正しくcatchしていない可能性。または `mockRejectedValue` → `mockRejectedValueOnce` に変更が必要
- 修正: mockRejectedValueOnce に変更して他のテストへの影響を防ぐ

### ルール
- テストコードのみ修正（プロダクションコードは変更しない）
- 既存のパスしているテストを壊さない
- listing-pipeline.test.ts は実DB必要なので無視

### 検証
```bash
cd ~/Desktop/rakuda
npx vitest run apps/api/src/test/integration/listings.test.ts apps/api/src/test/unit/listing-performance.test.ts apps/api/src/test/integration/settings.test.ts --reporter=verbose
```
