Task: Phase 0-C-4 - Migrate joom-publish-service.ts from JoomListing to Listing

File: apps/worker/src/lib/joom-publish-service.ts (862 lines)

This file uses prisma.joomListing for Joom listing CRUD operations.
We need to migrate these operations to use prisma.listing with marketplace='JOOM' and store Joom-specific data in the marketplaceData JSON field.

IMPORTANT RULES:
- DO NOT delete the JoomListing model from the schema
- DO NOT modify schema.prisma
- DO NOT run prisma commands
- Keep the JoomPublishBatch operations unchanged (they don't use JoomListing directly)
- Keep function signatures the same - only change the internal Prisma queries
- Ensure all existing functionality is preserved

Changes needed:

1. Replace all `prisma.joomListing.upsert()` with `prisma.listing.upsert()`:
   - Use `where: { productId_marketplace: { productId, marketplace: 'JOOM' } }` as the unique key
   - Move Joom-specific fields (joomProductId, joomVariantId, joomSku, joomCategory, joomAttributes, joomImages, dryRunResult, shippingPrice, shippingTime) into `marketplaceData` JSON
   - Map title/description to the Listing's marketplaceData as well
   - Map price to `listingPrice`
   - Map status: DRAFT -> DRAFT, READY -> PENDING_PUBLISH, PUBLISHING -> PUBLISHING, ACTIVE -> ACTIVE, PAUSED -> PAUSED, SOLD -> SOLD, ENDED -> ENDED, ERROR -> ERROR
   - Set `currency: 'USD'`
   - Set `publishedAt` and `lastSyncedAt` on the Listing model directly

2. Replace all `prisma.joomListing.findUnique()` with `prisma.listing.findUnique()`:
   - When finding by ID: use `where: { id: joomListingId }`
   - When using include for task data, use the product relation instead

3. Replace all `prisma.joomListing.update()` with `prisma.listing.update()`:
   - Map status changes
   - Move Joom-specific update data into marketplaceData merge
   - For errorCount and lastError, store in marketplaceData

4. Replace `prisma.joomListing.delete()` with `prisma.listing.delete()`

5. For queries that check `joomListing: null` (unlinked tasks), change to check that no Listing exists for productId with marketplace='JOOM'

IMPORTANT: When updating marketplaceData, you need to merge with existing data. Use this pattern:
```typescript
const existing = await prisma.listing.findUnique({ where: { id } });
const currentData = (existing?.marketplaceData as Record<string, any>) || {};
await prisma.listing.update({
  where: { id },
  data: {
    marketplaceData: { ...currentData, joomProductId: newId, ... },
  },
});
```

6. After all changes, run tests:
```bash
cd ~/Desktop/rakuda && npm test --workspace=apps/worker 2>&1 | tail -30
```
