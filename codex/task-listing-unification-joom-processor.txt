Task: Phase 0-C-6 - Migrate apps/worker/src/processors/joom-publish.ts from JoomListing to Listing

Replace all prisma.joomListing operations with prisma.listing using marketplace='JOOM'.

Key changes:

1. processImagesForListing(): prisma.joomListing.findUnique() -> prisma.listing.findUnique()
   - Access joomImages from (listing.marketplaceData as any)?.joomImages

2. processPublish(): prisma.joomListing.findUnique() -> prisma.listing.findUnique()
   - Access title from (listing.marketplaceData as any)?.title
   - Access price from listing.listingPrice

3. processSyncStatus(): prisma.joomListing.findUnique() -> prisma.listing.findUnique()
   - Access joomProductId from (listing.marketplaceData as any)?.joomProductId
   - For update, merge new data into marketplaceData:
     ```typescript
     const currentData = (listing.marketplaceData as any) || {};
     await prisma.listing.update({
       where: { id: joomListingId },
       data: {
         lastSyncedAt: new Date(),
         marketplaceData: { ...currentData, errorCount: ..., lastError: ... }
       }
     });
     ```

4. processAutoJoomPublish(): Change `joomListing: null` filter
   - Instead of checking enrichmentTask.joomListing == null,
     check if no Listing exists for productId with marketplace='JOOM':
     ```typescript
     const approvedTasks = await prisma.enrichmentTask.findMany({
       where: { status: 'APPROVED' },
       take: limit,
       orderBy: { createdAt: 'asc' },
       include: { product: true }
     });
     // Then filter out tasks where a JOOM listing already exists
     const tasksWithoutListing = [];
     for (const task of approvedTasks) {
       const existingListing = await prisma.listing.findFirst({
         where: { productId: task.productId, marketplace: 'JOOM' }
       });
       if (!existingListing) tasksWithoutListing.push(task);
     }
     ```

DO NOT modify schema.prisma. Only edit apps/worker/src/processors/joom-publish.ts.
DO NOT run tests.
