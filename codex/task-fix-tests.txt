## タスク: テスト修正（4件の失敗テスト）

### 修正1: apps/api/src/test/integration/products.test.ts

行26-30の `vi.mock('@rakuda/schema', ...)` に `SourceTypeSchema` を追加する。

現在:
```typescript
vi.mock('@rakuda/schema', () => ({
  ScrapedProductSchema: {},
  parseScrapedProduct: mockParseScrapedProduct,
  generateSourceHash: mockGenerateSourceHash,
}));
```

修正後:
```typescript
vi.mock('@rakuda/schema', () => ({
  ScrapedProductSchema: {},
  SourceTypeSchema: { parse: vi.fn((val: string) => val) },
  parseScrapedProduct: mockParseScrapedProduct,
  generateSourceHash: mockGenerateSourceHash,
}));
```

### 修正2: apps/api/src/test/unit/products.test.ts

行16-19の `listing` モックに `findFirst` を追加する。ルーターのbulk/publishエンドポイント（products.ts:1051）は `prisma.listing.findFirst` を使っているが、モックには `findUnique` しかない。

現在（行16-19付近）:
```typescript
listing: {
  findUnique: vi.fn().mockResolvedValue(null),
  create: vi.fn().mockResolvedValue({ id: 'l1' }),
},
```

修正後:
```typescript
listing: {
  findUnique: vi.fn().mockResolvedValue(null),
  findFirst: vi.fn().mockResolvedValue(null),
  create: vi.fn().mockResolvedValue({ id: 'l1' }),
},
```

### 確認事項
- この2つのファイルのみ変更すること
- 変更後に `cd ~/Desktop/rakuda && npx turbo run test --filter=@rakuda/api` で全テストがパスすることを確認
