## タスク: Worker Shopifyテスト修正

apps/worker/src/test/integration/shopify-publish-flow.test.ts の3テストが失敗している。

### 失敗テスト
1. "should create Shopify product from enriched RAKUDA product" → result.success がfalse
2. "should handle Shopify API rate limiting gracefully" → res.success がfalse
3. "should handle Shopify API error and set listing ERROR status" → listing.updateが呼ばれない

### 根本原因を調査
1. テストファイル: apps/worker/src/test/integration/shopify-publish-flow.test.ts を読む
2. テスト対象のサービス: shopifyPublishService のソースコードを読む
3. モック設定が正しいか確認
4. publishToShopify が false を返す原因を特定

### 修正ルール
- テストのモック/セットアップのみ修正する（テストの期待値を変えるのではなく、モックの不足を修正する）
- ソースコードは変更しない
- 修正後に `cd ~/Desktop/rakuda && npm test --workspace=apps/worker` で全テストが通ることを確認
