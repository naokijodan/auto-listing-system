# =============================================================================
# RAKUDA 本番環境設定テンプレート
# =============================================================================
# このファイルを .env.production にコピーして値を設定してください
# cp .env.production.example .env.production
# =============================================================================

# -----------------------------------------------------------------------------
# データベース
# -----------------------------------------------------------------------------
POSTGRES_USER=rakuda
POSTGRES_PASSWORD=your-secure-password-here
POSTGRES_DB=rakuda_production
DATABASE_URL=postgresql://rakuda:your-secure-password-here@postgres:5432/rakuda_production

# -----------------------------------------------------------------------------
# API設定
# -----------------------------------------------------------------------------
API_KEY=your-api-key-here
AUTH_ENABLED=true
CORS_ORIGINS=https://your-domain.com

# -----------------------------------------------------------------------------
# S3/MinIO (画像ストレージ)
# -----------------------------------------------------------------------------
# MinIO (自ホスト) の場合
S3_ENDPOINT=http://minio:9000
S3_ACCESS_KEY=minioadmin
S3_SECRET_KEY=minioadmin
S3_BUCKET=rakuda-images
S3_REGION=us-east-1

# AWS S3 の場合
# S3_ENDPOINT=https://s3.amazonaws.com
# S3_ACCESS_KEY=your-aws-access-key
# S3_SECRET_KEY=your-aws-secret-key
# S3_BUCKET=your-bucket-name
# S3_REGION=ap-northeast-1

# CDN URL (Joom出品用: 画像が外部からアクセス可能なURLが必要)
# MinIOを公開する場合: https://storage.your-domain.com
# AWS S3の場合: https://your-bucket.s3.ap-northeast-1.amazonaws.com
# Cloudflare R2の場合: https://your-bucket.r2.dev
CDN_URL=https://storage.your-domain.com

MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin-secret

# -----------------------------------------------------------------------------
# OpenAI (AI翻訳)
# -----------------------------------------------------------------------------
OPENAI_API_KEY=sk-your-openai-api-key

# -----------------------------------------------------------------------------
# eBay API
# -----------------------------------------------------------------------------
EBAY_ENV=production
EBAY_CLIENT_ID=your-ebay-client-id
EBAY_CLIENT_SECRET=your-ebay-client-secret
EBAY_REFRESH_TOKEN=your-ebay-refresh-token

# -----------------------------------------------------------------------------
# 通知設定
# -----------------------------------------------------------------------------
# Slack
SLACK_WEBHOOK_URL=https://hooks.slack.com/services/xxx

# Discord
DISCORD_WEBHOOK_URL=https://discord.com/api/webhooks/xxx

# LINE Notify
LINE_NOTIFY_TOKEN=your-line-notify-token

# メール (SMTP)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=your-email@gmail.com
SMTP_PASS=your-app-password
SMTP_FROM=noreply@your-domain.com
NOTIFICATION_EMAIL=admin@your-domain.com

# -----------------------------------------------------------------------------
# プロキシ設定 (スクレイピング用、任意)
# -----------------------------------------------------------------------------
# PROXY_HOST=your-proxy-host
# PROXY_PORT=8080
# PROXY_USER=proxy-user
# PROXY_PASS=proxy-pass

# -----------------------------------------------------------------------------
# フロントエンド
# -----------------------------------------------------------------------------
NEXT_PUBLIC_API_URL=https://api.your-domain.com
